# Spotify Developer credentials — register an app at
# https://developer.spotify.com/dashboard. No redirect URI needed; we only use
# the Client Credentials flow for public catalog data.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Optional. Railway sets PORT automatically; locally defaults to 3000.
PORT=3000
